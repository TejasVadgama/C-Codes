#include<stdio.h>

int main() {
    int i = true; //Here initalized the I Variable.

    if (i == true) // Check Condition Value if I True then Print inner Printf Function.
    {
        printf("i Value is True.");
    }else
    {
        printf("i Value is the False."); // if i false the condition then print else Statement.
    }
    return 0;
}